import { LandingPage } from "@/components/marketing/landing-page";

export const metadata = {
  title: "LOOP — Turn customer feedback into your next big move",
  description:
    "LOOP collects customer feedback and turns it into sentiment, themes, trends, and evidence-backed decisions.",
};

export default function Home() {
  return <LandingPage />;
}

